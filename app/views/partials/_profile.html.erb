<div id="profile-wrapper">
  <div class="Polaris-Card">
    <div class="Polaris-Card__Header">
      <h2 class="Polaris-Heading"><%= t('.profile')%></h2>
    </div>
    <div class="Polaris-Card__Section">
      <div class="Polaris-Scrollable Polaris-Scrollable--vertical" data-polaris-scrollable="true" style="height: 100px;">
        <dl class="Polaris-DescriptionList">
          <% [user.employee_record.attributes, user.attributes].each do |collection|%>
            <% collection.each_pair do |key, value|%>
              <% unless key == 'created_at' || key == 'updated_at' || key == 'id' || key == 'password_digest' || key == 'gravatar'%>
                <% if key == 'username'|| key =='date_of_certification'|| key == 'hours_per_week'|| key == 'night_shift'%>
                <dt class="Polaris-DescriptionList__Term flex1"><%=t(".activerecord.attributes.nurse.#{key}")%></dt>
                <% else %>
                <dt class="Polaris-DescriptionList__Term flex1"><%=t(".activerecord.attributes.employee_record.#{key}")%></dt>
                <% end%>
                  <dd class="Polaris-DescriptionList__Description flex2"><%=value%></dd>
              <%end%>
            <%end%>
          <%end%>
        </dl>
      </div>
    </div>
  </div>
</div>
